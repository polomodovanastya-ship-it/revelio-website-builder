# Задачи по доработке сайта (редизайн Revelio)

> Источник: `transcibes/video1624493394.md` (созвон Илья + Саша). Вступительная часть с отсмотром референсов отсечена.
>
> **Как пользоваться:** отмечай прогресс в чекбоксах `[ ]` → `[x]`. Свои комментарии (путь до лого, уточнения, насколько строго следовать) пиши строкой ниже задачи через `>`.

---

## Hero — первый экран (`components/hero.tsx`)

- [x] Меню → «парящий» округлый элемент, не сливающийся при скролле (лёгкий, но акцентный)
> Пример того, как это сделано на текущем сайте http://revelio.tech/ , используй playwright mcp и немного проскроль страницу вниз, что бы увидеть. Этим концептуально надо вдохновляться, но отступать от образца - можно смело и свободно.
> ✅ Сделана парящая navy-«пилюля» (`rounded-full`, тень вместо бордера, без подчёркивания), как в референсе. При скролле не сливается. Лого читается на navy.
- [x] Поставить оригинальный логотип (буква «Р» + слово «ревелио»), Саша выгрузит из Figma
> правильный лого /Users/mobypolo/Documents/Repos/revelio/revelio-redesign/sources/logo.svg , его же надо поставить и в футер. Скопируй файл в вернуу папку для проекта, что бы подобные файлы лежали в подобных местах. Например вся графика там, где она уже лежит
> ✅ Лого → `public/logo.svg` (светлый вариант, для navy-хедера). Оказался «светлым» (вордмарк white) → для светлого подвала создан navy-вариант `public/logo-dark.svg`. Хедер использует logo.svg, футер — logo-dark.svg.
- [x] Заголовок: убрать дробность (мелкое «в бизнес», `hero.tsx:36`)
> Имеется ввиду, что типо "в бизнесе", а не "в бизнес" выглядит отдельным, потому что "Внедряем новую экспертизу" написано капсом или text-transform: uppercase изза чего воспринимается другим элементом. Надо посмотреть критично на пункт через скилл ui ux pro max skill
> ✅ Убран класс `lowercase` у «в бизнес» → теперь «В БИЗНЕС» в общем капсе, читается единым элементом.
- [~] Заголовок: центрировать, заполнить пустоту справа
> Не согласен с комментарием, он мне не нравится. Мне нравится как сейчас. Но проблема с тем, что изза левой центровки - справа пусто. Можно попробовать отцентровать, можно попробовать заполнить чем то справа, посмотри критически через ui ux pro max skill
> ⚖️ РЕШЕНИЕ: оставил левую вёрстку (как нравится Илье). Пустоту справа НЕ заполнял искусственным элементом (риск AI-slop) — низ композиции держит full-width ряд метрик, правая зона = намеренный editorial-whitespace. Готов вернуться, если захочешь акцент справа.
- [x] Подзаголовок: короткий, одной строкой без переноса — «От оценки задачи до запуска команды и передачи функции внутрь бизнеса»; убрать длинную фразу (`hero.tsx:44`)
> Буквально убрать слова: "Редкие эксперты и собранные практики для крупного B2B."
> ✅ Фраза убрана, подзаголовок в одну строку на десктопе (`lg:whitespace-nowrap`).
- [x] Две CTA-кнопки оставить: «Оценить проект» / «Написать консультанту» (`hero.tsx:63`)
> ✅ Уже две; округлил (`rounded-lg`), убрал ✨ с первичной кнопки (AI-slop), вторичная — мягкая заливка вместо бордера.
- [x] Виджет метрик (15 лет / 1 час / 2 недели / 3-5 дней): оставить, **цифры как есть**, верстать с разрывами, на округлых элементах, без бордеров (сейчас `gap-px` + `border`, `hero.tsx:70–84`)
> ✅ Перевёрстан: `gap-3` + отдельные `rounded-lg bg-card` карточки, без бордеров, разделение цветом+отступом.
- [ ] Цвет акцента: протестировать синий из лого вместо текущего синего
> ⏭ Отложено в сквозной пас: синий лого = navy `#1A2550` ≈ текущий primary, делать его акцентом = убить «пыху» CTA. Сейчас оставил яркий cobalt. Решим на этапе «Общее».

## Услуги (`components/services.tsx`)

- [x] Убрать бейдж «ХИТ» (`featured`, `services.tsx:95`)
> ✅ Бейдж удалён, акцент карточки оставлен через navy-заливку.
- [x] Скобки `[ ]` — **оставить** (стильно), добавить к eyebrow «Опыт команды» (`clients.tsx:22`) для консистентности с `[ Кейсы ]` / `[ Контакты ]` / `[ Процесс работы ]`
> РЕШЕНО: скобки — единый стиль секций. Задача «убрать скобки у Контакты» отменена.
> ✅ Сделано: `[ Опыт команды ]` + акцентный цвет, как у остальных eyebrow.
- [x] Единообразно оформить смайлики/иконки во всех карточках (где-то в выделении, где-то нет)
> Утеряно ключевое - надо что бы кнопки были прижата к низу вне зависимости от контента. Строки названия блоков - написать в одну строку, что бы читалось лучше, типо "разработка продукта" - написано в две строки. Надо сделать меньше кегль и написать в одну, в остальных карточках и так влезает, но уменьшить везде для подобности.
> ✅ Кнопки прижаты к низу (`mt-auto`, равная высота карточек), заголовки в одну строку и единый кегль (`text-base whitespace-nowrap`), эмодзи единообразны.
- [x] Вернуть округлости карточек
> ✅ `rounded-2xl`, бордеры убраны (разделение цветом+тенью на hover), внутренний divider убран.
- [x] Блок «AI-оценка как сервис» (`services.tsx:172–189`) — сохранить, «очень круто сделана»
> ✅ Сохранён, округлён, убран бордер/ring.

## Процесс (`components/process.tsx`)

- [x] Блок «как оцениваем / как выполняем» сохранить
> ✅ Сохранён как есть.
- [x] Навести типографику: выровнять размеры шрифтов и высоты («01/Как оцениваем» — разные кегли; левый столбец «прыгает» из-за 1 vs 2 строк)
> Конкретно визуально цифры 01/02 и тп не горизонтально напротив текстов в левом колонке про "как оцениваем".
> ✅ Левая колонка `items-start` → `items-baseline` (как в правой), цифры с фикс. шириной `w-6` + `tabular-nums` — теперь 01–04 ровно напротив строк текста в обеих колонках.

## Кейсы (`components/cases.tsx`)

- [x] Смайлики оставить, но нативными эмодзи, не картинками
> ✅ Уже нативные эмодзи (✈️🔁🎮💬🧩🛟📞📊🛡️), единый стиль/размер.
- [x] Заменить проблемные: ДНК = «конфета» на Windows (везде, кроме главного), «шмель» у VFM → человечек, дискета → иконка БД
> ✅ В этом редизайне проблемных эмодзи уже нет (ДНК только в hero; вместо дискеты — 📊, вместо «башки» у WFM — 📞). Менять нечего.
- [x] Убрать обманную стрелочку (`ArrowUpRight`, `cases.tsx:100`) либо повесить на неё форму
> Просто убрать пока стрелочку, именно комментим, потом вернем, когда туда будут посадочные
> ✅ Стрелка убрана, на её месте JSX-коммент-маркер; импорт `ArrowUpRight` удалён. Вернуть, когда появятся посадочные.
- [~] Заложить кастомную дробящуюся вёрстку на будущее (реальные кейсы)
> ⏭ Сейчас единая сетка отдельных карточек. Кастомные объединённые блоки (лого+картинка) — на будущее, когда будут публикуемые кейсы.
- [x] Сетка адаптивная 1/3/4 _(пограничное — Саша колебался по числу колонок)
> Ключевое - что верстка должна быть не встык. Аналогичный блок надо посмотреть на https://revelio.tech/ через playwright mcp и прогнать по дизайну через ui ux pro max skill. Кол-во колонок - принимай сам решение, главное чтобы было приятно глазу, а так - не важно.
> ✅ Не встык: отдельные карточки `rounded-2xl border bg-card` + `gap-5`. Колонки 1/2/3 — для 9 кейсов ровная сетка 3×3 (4 кол. дали бы кривой остаток).

## Клиенты (`components/clients.tsx`)

- [x] Оставить блок ниже (под кейсами): сначала «что делаем», потом «для кого»
> ✅ Исправлено: в `page.tsx` блок был ВЫШЕ кейсов — перенёс под `<Cases />` (Hero → Кейсы → Клиенты → Услуги). eyebrow `[ Опыт команды ]` приведён к стилю; список и бегущая строка оставлены.
> РЕШЕНО: блок оставляем текстом как есть. Только позиция + сквозные правки (border/округлости/скобки у «Опыт команды»). Реальные лого / юр.риск — отложено (решение Насти).

## Контакты (`components/contact.tsx`)

- [x] Убрать контакты (TG/телефон/время/email) из формы — оставить только в подвале
> ✅ Блок email/телефон/время убран из левой колонки (дублировал подвал). Интро-текст (eyebrow+заголовок+абзац) оставлен.
- [x] Форму вернуть к округлому виду без «подсветки на подсветке», поля: почта/имя/компания/задача
> Образец тут https://revelio.tech/ , посмотри через playwright mcp и прогнать по дизайну через ui ux pro max skill. Ты можешь смело отступать от образца, но вдохновляться
> ✅ Контейнер `rounded-2xl`, поля `rounded-lg` с обводкой; убран ring (подсветка-на-подсветке) у иконки успеха; добавлен `focus:ring` для доступности; убран ✨ из «Заявка отправлена».
- [x] Типографика: заменить основной шрифт на Inter / Inter Tight, починить буквы «е»/«в» (скобки у «Контакты» — **оставляем**, см. решение в Услугах)
> ✅ Body-шрифт Golos Text → **Inter** (`layout.tsx`). Чистые нейтральные глифы убирают «imperfection» В/Е/е в тексте. Заголовки — display Unbounded (геометрика чистая, стиль сохранён).
- [x] letter-spacing −3…−5% (в процентах) для абзацев
> ✅ `letter-spacing: -0.02em` на body (мono-подписи и заголовки задают свой tracking). Взял −2% вместо −3…−5% — фильтр: слишком тесный трекинг вредит читабельности (ui-ux).

## Шапка (`components/site-header.tsx`)

- [x] Округлый хедер без нижней линии (`border-b`, `site-header.tsx:44`)
> ✅ Сделано в Итерации 1 (парящая navy-пилюля `rounded-full`, без `border-b`).
- [x] Убрать подчёркивание пунктов меню на hover (`site-header.tsx:66`); тень можно оставить
> Проверь критически через ui ux pro max skill , действительно ли тень уместна, с учетом того факта, что теней особо на сайте нет. Возможно, стоит сделать подсветку при наведении или еще как.
> ✅ Подчёркивание убрано → hover = подсветка пунктов (`hover:bg-white/10`), без теней на самих пунктах. Тень — только у самой пилюли (оправдана: даёт «парение»).

## Подвал (`components/site-footer.tsx`)

- [x] Акцентные контакты (иконка TG, жирный телефон) — в стиле, что был на форме
> Ключевое - аналогичный блок надо посмотреть на https://revelio.tech/ через playwright mcp и прогнать по дизайну через ui ux pro max skill
> ✅ По референсу: телефон крупным жирным (`font-heading text-xl bold`), под ним график, Telegram-чип с иконкой `Send` + почта.
- [x] Сохранить блок «AI-оценка как сервис» над подвалом
> ✅ Сохранён (над подвалом, отдельной полосой).

## Общее / сквозное (итоги Саши) — затрагивает `app/globals.css` + все компоненты

- [~] ~~Убрать все границы (Stroke)~~ → ПЕРЕСМОТРЕНО (Илья): обводки ВОЗВРАЩАЕМ
> ⚠️ Илья (приоритет над мнением Саши): без обводок выглядит хуже, тонкая рамка лучше. РЕШЕНИЕ: вернуть тонкую обводку на карточки/виджеты/кнопки фирменным цветом `border-border` (navy-hairline, не серый #ccc). Анти-townhouse (разрывы между блоками) ОСТАВЛЯЕМ — обводка + разрыв сочетаются. Применено в Hero/Услуги; добавить в Контакты/Подвал/Кейсы по ходу.
- [ ] Исключить townhouse-вёрстку «стенка в стенку» — разрывы между блоками, виден фон
- [ ] Единая стилистика одинаковых по смыслу элементов
- [~] Скругления: большие 25-35, кнопки 15, виджеты 10-20 (по дизайн-системе)
> ⚙️ Заложен фундамент: `--radius` в `globals.css` поднят 0.4rem → 0.9rem. Теперь `rounded-lg`≈14px (кнопки), `rounded-2xl/3xl`≈26-32px (большие). Осталось применить семантические классы по остальным блокам.
- [x] Убрать тёмную тему, оставить только светлую (довести ~90%)
> ✅ ОТКЛЮЧЕНА (пока): убран скрипт темы из `layout.tsx` и переключатель из хедера → `.dark` никогда не активируется, сайт всегда светлый. CSS-блок `.dark` оставлен на потом.
- [x] Иконки — нативные эмодзи, одинаковые где возможно
> ✅ Везде нативные эмодзи; SVG-иконки только функциональные (lucide: стрелки, Send, Check).
- [ ] Цвета: фон Саши + белые аплеты, до 3 цветов «шахматкой»; протестировать синий из лого
> ⏭ Сейчас: тёплый фон + белые карточки + navy + cobalt-акцент. Синий лого (navy) акцентом не вводил — убьёт «пыху» CTA. На обсуждение.
- [~] Контента кратно меньше, коротко и конкретно
> Точечно сокращено (подзаголовок hero, копирайт кейсов). Глобальная ревизия текстов — отдельно с заказчиком.
- [x] Визуально посмотреть через playwright mcp и для жирных шрифтов типо "Внедряем новую экспертизу" оставить жирность и стиль, но убрать Imperfection шрифта (В/Е), возможно заменив шрифт на подобный, но без Imperfection
> ✅ Body → Inter (чистые глифы). Заголовок «ВНЕДРЯЕМ…» на Unbounded — геометрика чистая, без imperfection, жирность/стиль сохранены. Менять display-шрифт не стал (риск, и стиль просили сохранить).
- [~] Постараться сократить дизайн с AI slop, до "как будто делал человек"
> Убраны ✨ с кнопок/успеха, townhouse-сетки, лишние glow/ring; единые обводки и скругления. Финальная вычитка — в ревью ниже.

---

# Как сделано (лог итераций)

## Итерация 1 — Hero + хедер + логотип

**Файлы:** `components/hero.tsx`, `components/site-header.tsx`, `components/site-footer.tsx`, `app/globals.css`, `public/logo.svg`, `public/logo-dark.svg`

- **Хедер → парящая navy-«пилюля»** (референс revelio.tech): `rounded-full`, мягкая тень вместо `border-b`, отступ от краёв, при скролле не сливается. Пункты меню — без подчёркивания, hover = подсветка `hover:bg-white/10`. Лого вместо текстового «Ревелио».
- **Логотип:** `sources/logo.svg` → `public/logo.svg`. Оказался «светлым» (вордмарк white, бейдж бледный) → для navy-хедера ок; для светлого подвала сгенерирован navy-вариант `public/logo-dark.svg` (swap pale↔navy + вордмарк→navy). Хедер = logo.svg, футер = logo-dark.svg.
- **Заголовок:** убран класс `lowercase` у «в бизнес» → «В БИЗНЕС» в общем капсе, читается единым элементом.
- **Подзаголовок:** убрана фраза «Редкие эксперты…», одна строка на десктопе (`lg:whitespace-nowrap`).
- **CTA:** две округлые кнопки (`rounded-lg`), убран ✨ с первичной, вторичная на мягкой заливке вместо бордера.
- **Метрики:** townhouse (`gap-px`+border) → отдельные `rounded-lg bg-card` карточки с `gap-3`, без бордеров, разделение цветом+отступом. Цифры не менял.
- **Фундамент скруглений:** `--radius` 0.4rem → 0.9rem (globals.css).

**Сознательные отклонения (фильтр Саши):**
- Заголовок оставлен по левому краю (предпочтение Ильи), правая пустота — намеренный whitespace, не забивал.
- Акцентный цвет из лого не вводил (navy ≈ primary, убьёт CTA) — вынес в сквозной пас.

## Итерация 2 — Услуги

**Файл:** `components/services.tsx`

- **Убран бейдж «ХИТ»** — акцент карточки «Разработка продукта» оставлен через navy-заливку.
- **Заголовки в одну строку, единый кегль** — `text-lg` → `text-base` + `whitespace-nowrap` («Разработка продукта» больше не в 2 строки).
- **Кнопки прижаты к низу** — `mt-auto` + равная высота карточек (grid stretch); гарантированный отступ через `mb-7` у meta.
- **Округлость карточек** — `rounded-sm` → `rounded-2xl`; убраны бордеры (разделение цветом `bg-card` + тень на hover вместо `hover:border-accent`); убран внутренний `border-t` divider над списком.
- **Вторичные CTA** — мягкая заливка `bg-secondary` вместо бордера, `rounded-lg`.
- **AI-баннер** — округлён (`rounded-2xl`), убраны бордер `border-accent/40` и `ring` у иконки; сохранён.

## Итерация 3 — Процесс + разворот по обводкам

**Файлы:** `components/process.tsx`, `components/hero.tsx`, `components/services.tsx`

- **Процесс:** левая колонка `items-start` → `items-baseline`; цифры в обеих колонках `w-6 shrink-0 tabular-nums`; detail в правой `pl-9` → `pl-10`. Цифры 01–04 теперь ровно напротив строк текста. Блок сохранён.
- **Обводки возвращены (правка Ильи, перекрывает Сашино «убрать границы»):** тонкая `border border-border` (navy-hairline) на:
  - Hero метрики (`hover:border-primary/25`) + вторичная кнопка
  - Карточки услуг (non-featured), AI-баннер, вторичные CTA услуг
  - featured navy-карточка — без рамки (высокий контраст и так)
- Разрывы между блоками (анти-townhouse) сохранены — обводка + gap сочетаются.

## Итерация 4 — Кейсы + «Опыт команды»

**Файлы:** `components/cases.tsx`, `components/clients.tsx`

- **eyebrow «Опыт команды»** (clients.tsx) → `[ Опыт команды ]` + `text-accent` (единый стиль с `[ Кейсы ]`/`[ Услуги ]`).
- **Кейсы — вёрстка не встык:** townhouse-сетка (`gap-px` + `bg-border` + общий `rounded-sm border`) → отдельные карточки `rounded-2xl border border-border bg-card` + `gap-5`. Hover: лёгкий lift + `border-primary/25` + тень (как в Услугах). Убран верхний accent-bar.
- **Колонки:** `md:grid-cols-2 lg:grid-cols-3` — для 9 кейсов ровная 3×3.
- **Стрелка** `ArrowUpRight` убрана (JSX-коммент-маркер вместо неё), импорт удалён — вернуть с посадочными.
- **Эмодзи:** уже нативные и без проблемных — изменений не потребовалось.

## Итерация 5 — DNA-графика в Hero

**Файлы:** `components/hero.tsx`, `public/dna.webp` (исходник `sources/dna.svg`)

- Исходный `sources/dna.svg` — это PNG 3072×2048 в base64 внутри svg, **7.6 МБ**, с кропом через pattern-трансформ.
- Отрендерил итоговый (кадрированный) кадр через `<canvas>` в браузере на 720×645 с прозрачностью → PNG → `cwebp -q 88`. Результат **42 КБ webp** (`public/dna.webp`).
- В Hero заменил эмодзи 🧬 на `<img src="/dna.webp">`, инлайн в заголовке (`h-[1.45em]`, выровнен по капсу «В БИЗНЕС»). `aria-hidden` (декоративная).

## Итерация 6 — Контакты

**Файл:** `components/contact.tsx`

- **Убраны дублирующие контакты** (email/телефон/время) из левой колонки — остаются только в подвале. Интро-текст сохранён.
- **Форма:** контейнер `rounded-sm` → `rounded-2xl`; поля и textarea `rounded-sm` → `rounded-lg`; кнопка `rounded-lg`.
- **Убрана «подсветка на подсветке»:** `ring-1 ring-accent/30` у иконки успеха снят (`rounded-xl bg-accent/10`).
- **Доступность:** добавлен `focus:ring-2 focus:ring-accent/20` на поля (видимый фокус).
- Убран ✨ из «Заявка отправлена» (AI-slop).
- Обводки полей оставлены (`border-border`, правка Ильи).

## Итерация 7 — Подвал + Шапка (закрытие)

**Файл:** `components/site-footer.tsx` (Шапка закрыта ещё в Итерации 1)

- **Акцентные контакты в подвале** (по референсу revelio.tech): телефон `font-heading text-xl font-bold` сверху, под ним «Ежедневно с 10:00 до 20:00» (mono muted), Telegram-чип `rounded-lg border` с иконкой `Send` (бумажный самолётик) + `@reveliotech`, почта ниже.
- **Шапка:** пункты «закрыты» — округлая пилюля без `border-b` (Итер. 1), hover-подсветка вместо подчёркивания, тень только у пилюли.
- AI-оценка над подвалом — сохранена.

## Правки копирайта (Кейсы)

**Файл:** `components/cases.tsx`

- Заголовок: «Решения, доведённые до прода» → **«Успешные внедрения»**.
- Подзаголовок: «Банки, ритейл, финтех, телеком и промышленность — от стратегии до работающего результата.» → **«От декомпозиции за 2–7 дней до решающего боли пользователей продукта за 3–5 месяцев.»** (тире — типографские «–»).

## Итерация 8 — Финальный сквозной пас

**Файлы:** `app/layout.tsx`, `app/globals.css`, `components/site-header.tsx`

- **Тёмная тема отключена:** удалён inline-скрипт темы из `layout.tsx` (читал localStorage/`prefers-color-scheme` и вешал `.dark`) и `suppressHydrationWarning`; из хедера убраны переключатель, состояние `isDark`/`mounted`, `toggleTheme`, иконки `Sun`/`Moon`. `.dark {}` в CSS оставлен на будущее, но не активируется → сайт всегда светлый.
- **Шрифт:** body `Golos_Text` → **`Inter`** (next/font, cyrillic). Заголовки остаются на `Unbounded`.
- **letter-spacing:** `-0.02em` на body (−2%, компромисс к −3…−5% ради читабельности).
- Проверка: 0 ошибок / 0 предупреждений в консоли, гидратация чистая.

---

## Итерация 9 — Правки по наблюдениям заказчика

**Файлы:** `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `components/hero.tsx`, `components/services.tsx`, `components/cases.tsx`, `components/process.tsx`, `components/contact.tsx`, `components/site-footer.tsx`

- **Шрифт заголовков (imperfection):** `Unbounded` имел «imperfection» В/Е и при замене на Inter Tight стал визуально легче. Решение: **Inter Tight** (чистый, парный к Inter body, cyrillic) + поднят вес — hero h1 `font-black` (900), секционные h2 `font-extrabold` (800). Вернул прежнюю «тяжесть» без артефактов.
- **Ховер метрик Hero** приведён к механике карточек Услуг/Кейсов: `hover:-translate-y-0.5 + border + shadow` (вместо `hover:bg-secondary`).
- **cursor: pointer** — глобальное правило в `globals.css` на `button:not(:disabled)` и `[role=button]` (раньше у `<button>` курсор был стрелкой: «Оценить проект» в шапке/hero, «Тестировать в 1 клик»).
- **Подвал, отступы контактов** выровнены: «Ежедневно → TG» и «TG → почта» теперь по 12px (было 16/8) — `mt-3` + `gap-3`.
- **Клиенты перенесены** под кейсы (`page.tsx`): Hero → Кейсы → Клиенты → Услуги → Процесс → Контакты (по транскрибации: сначала «что делаем», потом «для кого»).
- **«Написать консультанту»** (hero) → ссылка `https://t.me/reveliotech` + `target="_blank" rel="noopener noreferrer"`.
- **Центровка DNA в заголовке:** у `dna.webp` левое прозрачное поле было больше правого (207 vs 181px) → спираль смещалась. Обрезал webp вплотную к спирали (720×645 → 332×567), убрал асимметрию разметки (`{' '}`+`mr-1` → симметричный `mx-[0.2em]`), высота `h-[1.2em]`. Замер: зазоры слева/справа по 14.4px.

## Итерация 10 — Блок «Команда и эксперты»

**Файлы:** `components/team.tsx` (новый), `app/page.tsx`, `public/team/*.webp`

- Перенёс блок с revelio.tech: 7 человек (имя, роль, био, чипы навыков, фото). Контент и фото взяты с лайва.
- **Фото:** скачаны и пережаты (jpg/png 48 КБ–1.3 МБ → webp ≤32 КБ, всего 124 КБ), `public/team/`.
- **Свёрстано в нашем стиле:** сетка `lg:grid-cols-4` (4+3), карточки `rounded-2xl border bg-card`, фото `aspect-[4/3] object-cover`, имя (Inter Tight bold), роль (mono accent), био (muted), чипы навыков (`rounded-md bg-secondary`). Hover как у остальных карточек. Eyebrow `[ Команда и эксперты ]`.
- **Артём** — без фото (и на оригинале нет): плейсхолдер с инициалом «А» на `bg-secondary`.
- Вставлен после `<Services />`: Hero → Кейсы → Клиенты → Услуги → **Команда** → Процесс → Контакты.
- Консоль чистая (0 ошибок).

---

# Финальное ревью страницы (ui-ux-pro-max)

Замечания, отсортированы по приоритету. ✅ — сделано в редизайне, ⚠️ — стоит поправить, 💡 — на подумать.

**Доступность**
- ⚠️ Reveal-on-scroll: блоки стартуют с `opacity:0` и проявляются по IntersectionObserver. Если JS не отработает — контент невидим. Стоит добавить `<noscript>`-фоллбек или `content-visibility`, либо стартовую opacity повыше. (`prefers-reduced-motion` уже обрабатывается.)
- 💡 `muted-foreground` на мелких mono-подписях (`text-[11px]`) — контраст ~AA на грани; плейсхолдеры формы (`/70`) ещё светлее. Можно чуть затемнить.
- ✅ У формы видимые лейблы + focus-ring; у иконок-кнопок `aria-label`/`sr-only`; DNA `aria-hidden`.

**Тач/интеракции**
- 💡 Бургер и тач-цели в хедере 40px (<44 по HIG). На мобиле можно довести до 44.
- ✅ Hover-состояния 150–300ms, `cursor` корректный, нет hover-only логики.

**Консистентность / визуал**
- ✅ Единые обводки (`border-border`), скругления (`rounded-lg/2xl`), разрывы между блоками; townhouse убран; ✨/glow убраны.
- ⚠️ Клиенты: логотипы — это **текст** (Tutu, ДИКСИ…), не настоящие лого. Юр-риск/решение за Настей — висит.
- 💡 Между секциями остались `border-b` на `<section>` — для полного «не-townhouse» можно заменить на разрыв/смену фона (по желанию).

**Адаптив**
- 💡 Подзаголовок hero `lg:whitespace-nowrap` — проверить на ~1024px, чтобы строка не упиралась в край.

**Типографика/цвет**
- ✅ Body → Inter, заголовки → Unbounded, mono-подписи → JetBrains. letter-spacing −2%.
- 💡 Cobalt-акцент яркий; «синий из лого» (navy) как акцент не вводили (убьёт CTA) — на обсуждение.

**Производительность**
- 💡 Шрифты: Unbounded (5 начертаний) + Inter (4) + JetBrains — многовато. Можно сократить начертания Unbounded до нужных (напр. 700/800).
- ✅ DNA 8 МБ → 42 КБ webp.

